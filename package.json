{
  "name": "todo-life-dashboard",
  "version": "1.0.0",
  "description": "A zero-dependency client-side productivity dashboard",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
